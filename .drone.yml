kind: pipeline
type: kubernetes
name: analytics-ci

steps:
  - name: test
    image: python:3.12-slim
    commands:
      - pytest -q
  - name: build
    image: plugins/docker
    settings:
      repo: pathfindertestorg/analytics-api
      tags: [latest]
